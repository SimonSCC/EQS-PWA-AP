<Window x:Class="PWA_AccessLocal_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PWA_AccessLocal_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="AntiqueWhite">
    <Grid>
        <StackPanel Margin="25 25" HorizontalAlignment="Center">
            
            <StackPanel Name="SSIDInputPanel" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Network name" HorizontalAlignment="Center" TextAlignment="Right"  VerticalAlignment="Center" Width="125"></TextBlock>
            <TextBox  Width="250"  VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="NetworkName_Textbox" Margin="5" Text="{Binding Path=CurrentConfig.SSID}"/>
            <TextBlock IsHitTestVisible="False" Text="Network name" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="-255,0,0,0" Foreground="DarkGray">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=NetworkName_Textbox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            </StackPanel>

            <StackPanel Name="PassphraseInputPanel" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Network passphrase" HorizontalAlignment="Right" VerticalAlignment="Center" Width="125" TextAlignment="Right"></TextBlock>
                <TextBox  Width="250" VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="NetworkPass_Textbox" Margin="5" Text="{Binding Path=CurrentConfig.PassPhrase}"/>
            <TextBlock IsHitTestVisible="False" Text="Network Password" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="-250,0,0,0" Foreground="DarkGray">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=NetworkPass_Textbox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>                
            </StackPanel>
            <TextBlock Text="*Minumum 8 characters" Margin="0 -5 0 0" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>

            <TextBlock Margin="15 15 15 0" Text="{Binding HotSpotStatusMessage}" TextAlignment="Center"></TextBlock>
            
            <Button IsEnabled="{Binding StartHotspotBtnEnabled}" HorizontalAlignment="Center" Margin="0 5 0 5" Width="100" Content="Start hotspot" Command="{Binding StartHotspotCommand}"></Button>
            <Button IsEnabled="{Binding StopHotspotBtnEnabled}" HorizontalAlignment="Center" Margin="0 0" Width="100" Content="Stop hotspot" Command="{Binding StopHotspotCommand}"></Button>
            <TextBlock Text="Link to potential hosted PWA:" Margin="0 15 0 15" HorizontalAlignment="Center" Visibility="{Binding PWAInfo.LinkToPWA, Converter={StaticResource NullToVisibilityConverter}}"></TextBlock>

            <Button Name="HyperlinkPWA" Style="{StaticResource HyperlinkStyledButton}" Command="{Binding ClickLink}" Content="{Binding PWAInfo.LinkToPWA}" HorizontalAlignment="Center"></Button>
            
        </StackPanel>
    </Grid>
</Window>
